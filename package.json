{
  "name": "diabetes-expert-system",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build -- --outDir ../dist --emptyOutDir"
  }
}
