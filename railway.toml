[deploy]
startCommand = "gunicorn --chdir backend 'app:create_app()' --bind 0.0.0.0:$PORT --workers 4 --threads 2 --timeout 90"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
